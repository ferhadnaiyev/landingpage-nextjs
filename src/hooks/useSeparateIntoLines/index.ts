export const separateIntoLines = (text: string, lineCount: number) => {
    const result: string[] = []
    const wordCount = text.split(" ").length;
    const wordCountEachLine = wordCount / lineCount

    for i in range

    // const lineLength = text.length / lineCount;
    // const spaceIndex = text.indexOf(" ", lineLength);

    // if (spaceIndex === -1) return text;

    // return [text.slice(0, spaceIndex), text.slice(spaceIndex + 1)];
};
